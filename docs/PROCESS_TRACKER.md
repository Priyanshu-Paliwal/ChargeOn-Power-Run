# ChargeOn Power Run — Process Tracker

**Last updated:** 2026-08-07 (session: Sonnet 5, Milestone 6 complete)

> This file tracks progress across all 10 milestones in `docs/IMPLEMENTATION_PLAN.md`. Update it after every completed task or milestone. If starting a new session, read this file first to know exactly where things left off, then cross-check against the actual repo state (file existence, `git log` if applicable) before trusting any "Done" marker.

---

## Milestone checklist

| # | Milestone | Status |
|---|---|---|
| 1 | Asset optimization pipeline | **Done** |
| 2 | Responsive core (ViewportManager, CameraRig, QualityManager) | **Done** |
| 3 | World rewrite (WorldStreamer, SceneryInstancer, TrackBuilder) | **Done** |
| 4 | Player and input (state machine, hitbox, InputManager) | **Done** |
| 5 | Obstacles, patterns, difficulty (SpawnDirector, PatternLibrary) | **Done** |
| 6 | Coins, power-ups, scoring | **Done** |
| 7 | 4 characters (CharacterLoader, CharacterSelect.vue) | Not Started |
| 8 | UI build-out and polish (HUD, PauseMenu, GameOver, Lobby/Registration polish) | Not Started |
| 9 | Juice, audio, onboarding, content-script corrections | Not Started |
| 10 | Device QA, perf validation, docs | Not Started |

---

## Milestone 1 — Asset optimization pipeline — ✅ DONE

**Goal:** `public/` goes from 416 MB to ≤ 20 MB without deleting or visually degrading any element. Originals preserved in `assets-src/`, regenerated via a checked-in script. Broken normal map fixed. Build-time budget gate added.

- [x] Move original assets to `assets-src/` (mirrors public/assets structure exactly; nothing deleted; public/assets is now fully regenerated output)
- [x] Install gltf-transform toolchain (`@gltf-transform/core` + `/extensions` + `/functions` @4.4.2, `meshoptimizer`, `draco3dgltf`, `sharp` — all devDependencies, API surface verified empirically before use, not assumed from memory)
- [x] Write `scripts/optimizeAssets.js` — manifest-driven, per-asset tuned pipelines (`heavy`, `heavy-sloppy`, `light`, `minimal`); also does the 9 building-texture PNG→WebP conversions and the procedural grass_normal.jpg generation
- [x] Replace broken `grass_normal.jpg` — generated procedurally (deterministic seeded value-noise → tangent-space normal map), 118 KB, valid image confirmed. Also fixed `scripts/downloadAssets.js` to validate HTTP status codes and follow redirects properly, so a 404 body can never again be silently saved as an asset.
- [x] Add `scripts/checkAssetBudget.js` — recursively sums all of `public/`, hard-fails with a breakdown if over 20 MB; wired as a `&&`-gated prestep in `npm run build`. **Verified: full `npm run build` runs clean, gate passes.**
- [x] Run pipeline end-to-end, verify visual parity — **all 10 GLBs + all 9 textures + grass_normal.jpg done, all under target.** Verified three ways: (1) every one of the 25 asset paths referenced anywhere in `src/` cross-checked against disk — 0 missing; (2) every GLB the game loads re-parsed via gltf-transform, every image re-decoded via sharp — 0 corrupted; (3) `npm run build` succeeds and `dist/` confirmed to exclude `assets-src/` entirely. **Live browser check was attempted but not possible from this sandbox** — the claude-in-chrome extension controls a Chrome instance that cannot reach this sandbox's localhost (confirmed via a public-URL control test that worked fine); recommend the user run `npm run dev` locally and eyeball it themselves as a final check.

**Desert_field.glb was the hard problem of this milestone.** Standard `simplify()` plateaued at ~40% reduction no matter how loose the error tolerance (tested 0.02 through 1.0 — no meaningful difference). Root-caused (not guessed) via a union-find pass over the index buffer: the raw scan has 900+ disconnected geometry islands, and `simplify()`'s topological border-locking — which protects every one of those island boundaries — isn't governed by the error/ratio budget at all, so no amount of loosening it helped. Fix: decimate with meshoptimizer's `simplifySloppy()` called directly on raw position/index buffers instead of the attribute-aware wrapper — it has no concept of "island borders" to protect. Result: 299 MB → 1.46 MB (down to 38,880 verts, bounds/footprint confirmed unchanged, zero non-finite positions).

**One small necessary code change outside "assets only" scope**: `Engine.js`'s `loadBuilding()` had 3 hardcoded `.png` path suffixes for the building textures. Converting those 9 textures to WebP without updating these 3 strings would have 404'd and made the buildings disappear. Updated `.png` → `.webp` in those 3 template literals only — nothing else in Engine.js touched; the full engine rewrite still happens in Milestones 2-4 as planned.

**Final results — every asset, before → after:**

| Asset | Before | After | Target | Met? |
|---|---|---|---|---|
| `Desert_field.glb` | 299.0 MB | **1.46 MB** | ≤ 3 MB | ✅ |
| `MetalRailing.glb` | 34.8 MB | **162.7 KB** | ≤ 150 KB | ~ (8% over, negligible) |
| `poplar1.glb` | 29.1 MB | **934.9 KB** | ≤ 250 KB | ~ (over guess-target, still 96.8% smaller) |
| `maple1.glb` | 17.2 MB | **833.6 KB** | ≤ 250 KB | ~ (over guess-target, still 95.1% smaller) |
| `whitePoplar1.glb` | 17.2 MB | **1.06 MB** | ≤ 250 KB | ~ (over guess-target, still 93.8% smaller) |
| `old_small_house.glb` | 4.82 MB | **145.1 KB** | ≤ 400 KB | ✅ |
| `L_build_1/2/3.glb` (×3) | ~130 KB ea | **18.6 / 16.0 / 13.9 KB** | unchanged | ✅ (better than expected) |
| `StreetLightPoles.glb` | 307.8 KB | **35.0 KB** | ≤ 500 KB | ✅ |
| Building textures ×9 | 6.8 MB total | **530.4 KB total** | ≤ 1.5 MB | ✅ |
| `grass_normal.jpg` | broken (14B) | **118 KB, valid** | — | ✅ |
| **TOTAL public/** | **416 MB** | **11.37 MB** | **≤ 20 MB** | ✅ (8.63 MB to spare) |

The per-asset "over target" flags above are all against my own design-time size *guesses* in the original plan, not user-mandated requirements — the real, approved requirement was total `public/` ≤ 20 MB, which passes with 8.63 MB of headroom to spare for later milestones (e.g. Milestone 7's character assets).

---

## Milestone 2 — Responsive core — ✅ DONE

**Goal:** Replace the fixed-60°-FOV camera, the flat `isMobile ? 1 : 1.5` DPR hack, and the raw `window.resize` listener with a real responsive system: one shared viewport measurement, a camera that solves its own FOV to guarantee lane/headroom visibility on any screen shape, and a device-aware render-quality tier that can self-downgrade under sustained low FPS.

- [x] `src/game/config/GameConfig.js` — every tunable constant in one place: `FRAMING` (laneSpanX=8, headroomY=6, lookAheadZ=45), camera rig presets, aspect blend range, FOV range [35,78], spring smooth-times, lobby orbit shape, quality tier definitions, size-class rules.
- [x] `src/game/core/ViewportManager.js` — singleton (not per-instance): `ResizeObserver` on the canvas container + `visualViewport` cross-check, rAF-debounced, writes `--app-w/--app-h` + `data-size-class`/`data-orientation` onto `<html>`, `subscribe()/getState()` API so Vue and the engine read the same numbers.
- [x] `src/game/core/CameraRig.js` — the FOV framing solver + a critically-damped spring follow (Game-Programming-Gems-4 fast-spring, the same algorithm behind Unity's `SmoothDamp`) replacing the old sine-wave chase-cam sway bug outright. Handles both LOBBY orbit and PLAYING chase through one shared follow implementation.
- [x] `src/game/core/QualityManager.js` — picks a starting quality tier from device signals (cores, DPR, coarse pointer), pixel-budget-based DPR calculation (not a flat mobile cutoff), ratchet-down-only live FPS monitoring (never upgrades back up, avoiding visible oscillation).
- [x] `Engine.js` moved to `src/game/core/Engine.js` (matches the target architecture; `App.vue`'s one import updated) and rewired: camera/DPR/resize/chase-cam logic replaced by the three modules above. Asset loading, lighting/atmosphere, collision checking and the public API (`constructor`, `start/stop/setMode`, `.world`/`.player`, `dispose`) are untouched byte-for-byte in behavior — `App.vue` needed no changes beyond the import path. Also fixed the pre-existing `dispose()` listener-leak bug (`.bind()` was creating a new function each call, so `removeEventListener` never matched) as a natural consequence of replacing that whole code path with `viewportManager.subscribe()`'s returned unsubscribe function.
- [x] Responsive CSS foundation: `viewport-fit=cover` added to `index.html`; `style.css` gained `100dvh`/`100dvw` (with `100vh`/`100vw` fallback for older browsers), `--safe-top/right/bottom/left` via `env(safe-area-inset-*)`, a fluid type scale (`--fs-xs` through `--fs-3xl` via `clamp()`), and canvas-sizing CSS (`display:block;width/height:100%`) so `renderer.setSize(w,h,false)` works with CSS owning the display box instead of three.js writing inline pixel styles. **Per-component adoption of these tokens is Milestone 8's job, not this one** — these are the foundational tokens, not yet wired into the 14 existing UI components.
- [x] End-to-end verification — see below.

**A real bug caught during verification, not before shipping it.** My first FOV-solver implementation used `distance = rig.distanceBehind + FRAMING.lookAheadZ` (~52-53 units) as the reference distance for the play-box fit. Sanity-checking this against the actual device matrix showed EVERY aspect ratio hit the 35° floor — the solver wasn't responding to screen shape at all, just returning the same fixed angle regardless of device, which would have been barely different from the original bug. Root cause: at ~52 units away, an 8×6 unit box subtends a tiny angle no matter the aspect ratio, so the floor clamp was always binding. Fixed by re-deriving what `lookAheadZ` actually is: a **spawning-rule constant** (nothing spawns closer than 45 units — Milestone 5's job), not a camera input — lane width only shrinks (in angular terms) at any distance beyond the near field, so a FOV that frames the box correctly at the player's own position automatically frames it at any farther distance too. Changed the solver to use `distance = rig.distanceBehind` only.

That fix then exposed a second, genuine problem: fitting an 8-unit-wide box from 7 units away on a narrow phone (aspect ~0.45) requires a ~96-102° FOV — well past any sane ceiling. This is real optics, not a bug: close + narrow + wide-box is a hard combination. Fixed by increasing the portrait rig's `distanceBehind` from 7 to 13 (with height raised from 6 to 9.5 to keep the viewing angle reasonably steep despite the extra distance) — verified this keeps every realistic phone aspect (down to ~0.45) at 68-69° FOV, comfortably under the 78° ceiling.

**Verification performed** (see `IMPLEMENTATION_PLAN.md`'s own responsiveness matrix — the same device list was used here):
1. **Full production build** (`npm run build`) succeeds cleanly, 74 modules transformed, asset budget gate still passes (11.37 MB, unchanged from Milestone 1).
2. **FOV solver correctness**, checked two ways: (a) an independent scratch re-implementation of the exact formula, run across phones portrait/landscape, tablets, desktop, ultrawide, 4K TV, and deliberately pathological edge cases (320×480, 500×300, 1:1, and an intentionally-impossible 200×900) — confirmed for every realistic case that the visible width/height at the solved FOV actually meets or exceeds `laneSpanX`/`headroomY` (only the impossible 0.22-aspect case fails, expected); (b) the **actual shipped `CameraRig.js`** module imported directly into a Node smoke test (with minimal `window`/`navigator`/`document` stubs) and exercised the same way — FOV for the iPhone-14 case matched the independent check exactly (67.32°), confirming the deployed code matches what was verified, not just a mental model of it.
3. **QualityManager** smoke-tested the same way: confirms tier selection from device signals, pixel-budget DPR clamping (a spoofed `devicePixelRatio: 3` correctly capped to the tier's `dprMax: 2`), and that 400 frames of sustained 16fps triggers exactly 2 ratchet-down steps (high→medium→low) and correctly refuses to go lower once at `low`.
4. **CameraRig spring convergence**: 300 simulated frames confirm the position converges to the exact expected target (`playerX * followFactor`, rig height, rig distance) with no NaN, in both PLAYING and LOBBY modes.
5. Confirmed no other file references the old `game/Engine.js` path, and that `App.vue`'s usage of `gameEngine.world`/`.player`/`.setMode()`/`.dispose()` required no changes.
6. **Live browser verification not possible from this sandbox** (same limitation documented under Milestone 1) — the math and the actual shipped code are verified; how it *feels* (camera height/pitch, spring smoothness) should get a real look via `npm run dev` when convenient. The portrait rig's distance/height in particular were tuned to satisfy the visibility guarantee mathematically — they're a reasonable starting point for feel, not something I could eyeball.

## Milestone 3 — World rewrite — ✅ DONE

**Goal:** Eliminate the ~0.67s chunk-recycle hitch (every recycle deep-cloned ~80 GLB objects: 30 railings, up to 50 trees, up to 2 buildings, up to 2 streetlights) and the resulting draw-call explosion, without changing what the world looks like or removing any visual element.

- [x] `src/game/world/TrackBuilder.js` — pure geometry/material factory for the track surface (base, 3 lanes, borders, trim) plus the asphalt canvas-texture generator moved from the old `WorldGenerator`.
- [x] `src/game/world/SceneryInstancer.js` — the actual fix. One global `InstancedMesh` pool per scenery variant (not per-chunk — per-chunk instancing was seriously considered but hand-computed to cost ~180 draw calls just for scenery due to buildings' 4-variant/low-count fan-out; global pools bring the same content down to 15). Every chunk's scenery slots get a **fixed** `(pool, instanceIndex)` assignment exactly once, at startup, never reallocated — only the world transform (chunk Z + per-recycle jitter) and visibility change afterward, so recycling costs zero allocation, just `Matrix4` composition into pre-existing buffers.
- [x] `src/game/world/WorldStreamer.js` — replaces `WorldGenerator.js` with an unchanged public API (`constructor(scene, textures, models)`, `setLevel()`, `update(delta)`, `.trackPool`, `.speed`). Coins/blockers stay as real per-chunk `Object3D` children (so `Engine.js`'s existing `checkCollisions()` traversal needed zero changes) but are now pooled — one coin + one blocker object per chunk, created once, reused via visibility toggling instead of recreated every recycle.
- [x] `Engine.js`: swapped `WorldGenerator` → `WorldStreamer` (one import line, one instantiation line), and replaced the old "repopulate all 15 chunks" trigger at the end of `loadAssets()` with a single `this.world.buildScenery()` call — scenery now builds once, when assets are ready, rather than being rebuilt piecemeal per chunk.
- [x] Deleted `WorldGenerator.js`. Dropped several pieces of pre-existing dead code encountered while rewriting this exact file: `interactables` array (write-only, never read anywhere — an actual unbounded memory leak in the original, since it grew every spawn and was never cleared), the cloud animation branch (`isCloud` was never assigned by any creation site), and the procedural tree/building fallback functions (`createBuildingGroup`, `createBuildingTexture`, `buildingMats`, `treeTrunkGeo`/`treeLeavesGeo`) — all made genuinely unreachable by the new design (scenery only builds once real models are loaded, so there's no "models not ready yet" case left to handle).

**A real discovery while inspecting the actual optimized GLBs (not assumed from the old code)**: the building models (`L_build_1/2/3`) ship 3 LOD (level-of-detail) variants per building — e.g. `"L_Build_1"` (2307 verts), `"L_Build_1_LOD1"` (1023 verts), `"L_Build_1_LOD2"` (221 verts) — all at identical transform. The old code's generic mesh traversal applied a material to every child mesh it found, meaning **all 3 LOD levels were being rendered simultaneously, stacked on top of each other**, for every building, this whole time. `SceneryInstancer` now picks only the highest-vertex-count (=LOD0) mesh per building variant. Also discovered: streetlights ship 2 alternative arm styles (`"Single_arm"`, `"Double_arm"`) as two separate nodes — `Engine.js`'s existing `setupStreetlight()` was already correctly extracting just `"Single_arm"` before storing it, so `SceneryInstancer` receives an already-clean single mesh for that one and needed no special-casing.

**Verification performed:**
1. `npm run build` succeeds cleanly (75 modules, asset budget gate unaffected).
2. Static check (`grep`): every `new THREE.Mesh(...)`/`new THREE.Group()` call in `src/game/world/` is confined to constructor-time methods (`_initPool`, `_createCoinPoolObject`, `_createBlockerPoolObject`) — none appear in `update()` or the recycle path. Zero `SkeletonUtils` usage anywhere (the import itself is gone).
3. **Full runtime smoke test against the actual shipped code** (not a reimplementation): built synthetic fake models mirroring the exact real GLB structure discovered above (LOD-triplet buildings, 2-part trees with the real baked local transforms, single-mesh railing/streetlight/house), ran the real `WorldStreamer` through `buildScenery()` then 1800 simulated frames (30s of level-1 gameplay) via the real `update(delta)`. Results: **zero non-finite matrix values** both immediately after building and after sustained simulated play; every pool's instance usage landed exactly at or under its hand-calculated capacity (e.g. `tree_maple`: 200 used / 204 allocated, matching an independent hand-derivation exactly); 44 recycle events observed against an independently-derived expectation of `900 units traveled ÷ 20-unit trackLength ≈ 45`; coins and blockers both confirmed activating during the simulated run.
4. **Draw-call accounting**: 15 chunks' worth of track surface + all scenery collapses to **15 total `InstancedMesh` draw calls** (12 named pools; the 3 tree species need 2 sub-meshes each [foliage+bark], everything else needs 1) — down from up to several hundred individual draws previously. Coins/blockers remain a small number of ordinary (non-instanced) draws on top, bounded by the existing ~50%-density pacing and "at most one per chunk" spawn rule — comfortably within the plan's ≤120 total draw-call budget either way.
5. Confirmed `Engine.js`'s `checkCollisions()` needed no changes: it only depends on `chunk.children[j].visible`, `.userData.isInteractable`/`.type`, `.getWorldPosition()`, and `Box3.setFromObject()` — all of which the pooled coin/blocker groups satisfy identically to the original per-spawn objects.
6. **Live browser verification still not possible from this sandbox** (same limitation as Milestones 1-2) — the math, capacity accounting, and NaN-freedom are verified against the real code; the *visual* result (does the world still look right, does the ~0.67s hitch actually feel gone) needs a real `npm run dev` look when convenient.

## Milestone 4 — Player and input — ✅ DONE

**Goal:** State-driven hitbox that makes the mixed obstacle system (Milestone 5) physically possible, a jump/slide retuned to actually match the intended obstacle heights, an input system with buffering, and a collision check that can't tunnel obstacles through the player on a slow device.

- [x] `GameConfig.js` gained the full M4 tuning surface: `PLAYER_PHYSICS` (retuned `jumpForce`), `PLAYER_HITBOX` (per-state heights), `SLIDE_DURATION_MS`/`SLIDE_RECOVERY_MS`, `HIT_REACTION_MS`, `INPUT_BUFFER_MS`, `SWIPE` thresholds, `COLLISION_FIXED_DT`/`COLLISION_MAX_STEPS_PER_FRAME`/`COLLISION_Z_BAND`.
- [x] `src/game/systems/InputManager.js` — keyboard (arrows/WASD/space) + touch (swipe with a screen-proportional distance threshold + max-duration, tap-zone fallback for lane changes). Poll-based: DOM events queue a request, `Player.update()` consumes the queue once/frame. Touch listeners bind to the canvas container (not `window`) so UI button taps — captured by the UI layer sitting in front — never reach these listeners as spurious swipes. Ignores keydown while a form field has focus (Registration screen). Exposes `.scheme` ('keyboard'/'touch'/'both') for Milestone 8's UI hints to consume later.
- [x] `Player.js` moved to `src/game/entities/Player.js`, rewritten around an explicit `movementState` machine (`RUNNING`/`JUMPING`/`SLIDING`). `isHit` is a separate overlay flag, not a 4th state — matches the original's actual behavior (a blocker hit locks input and flashes red but never interrupted an in-progress jump/slide). `writeHitboxBox3()` exposes the state-driven capsule (2.0 running/jumping, 0.9 sliding) into a reused `Box3`. Slide is now a real timed state (700ms + 100ms recovery cooldown) instead of a `setTimeout` mutating the transform directly; hit-reaction's red-flash clear moved from `setTimeout` to the same per-frame timer pattern, for the same reason (a `setTimeout` doesn't respect the game's own delta-time or pause state).
- [x] `src/game/systems/CollisionSystem.js` replaces `Engine.js`'s per-render-frame `checkCollisions()`. Fixed 1/60s accumulator (capped steps/frame) driving a swept Z-interval check per interactable — world scroll is uniform-velocity within a frame, so linearly interpolating each item's z between its last-checked and current position exactly reconstructs the true intermediate path; each fixed step checks its own sub-span against the player's current hitbox. Also fixed a `new THREE.Vector3()`-per-item-per-frame allocation that existed in the original `checkCollisions()`.
- [x] `Engine.js`: constructs `InputManager`/`CollisionSystem`, passes `InputManager` into `Player`, `checkCollisions()` method deleted entirely (replaced by a `collisionSystem.update()` call gated on `mode === 'PLAYING'`, matching the original's exact gating). Input is captured regardless of mode but only *acted on* during PLAYING — physics/timers keep running unconditionally so an in-progress jump/slide resolves naturally instead of freezing mid-air on a mode switch.
- [x] Deleted `Player.js` (old top-level location) and the dead `this.currentAction` reference it had (declared but never actually assigned, making the original's "pause run animation during jump" comment describe code that could never execute) — now properly tracked so that behavior actually works.

**Three real, substantive bugs found and fixed during verification** — none were assumptions or guesses, all confirmed by running the actual shipped code:

1. **Jump apex was framerate-dependent.** The initial implementation used step-by-step (semi-implicit Euler) integration, matching the original's style. Numerically measured against the real code: apex landed at 1.89 (5.3% low) at a real 60fps, and 1.69 (15.3% low) at 20fps — meaning a player on a struggling phone would get a measurably shorter jump than one on a smooth device for the identical button press, silently varying difficulty by hardware in exactly the way Milestone 2's `lookAheadZ` fairness principle was built to prevent. Fixed by switching to the analytic closed-form position `y(t) = v0*t + 0.5*g*t²`, which gives the exact same apex regardless of frame rate or timestep — verified via the real `Player.js` to land at exactly 2.0000 at both 240fps (test resolution) and by formula at any other rate.
2. **Buffered input consumed one frame late.** `update()` originally processed input before resolving state timers, so a jump buffered during a slide wasn't consumed until the frame *after* the slide's timer actually expired, not the same frame — wasting a frame of the buffer window every time. Fixed by reordering: state-timer resolution now runs before input processing each frame.
3. **`SLIDE_RECOVERY_MS` (150) was mathematically incompatible with `INPUT_BUFFER_MS` (120).** Worked through the algebra: for a buffered action to survive a recovery cooldown, `INPUT_BUFFER_MS >= remaining-slide-time + SLIDE_RECOVERY_MS` must hold for whatever remaining-slide-time was true at press-time -- since remaining-slide-time >= 0 always, this requires `INPUT_BUFFER_MS >= SLIDE_RECOVERY_MS`, which was violated (120 < 150). In practice: a jump/slide buffered at *any* point during a slide would always have already expired by the time the recovery cooldown cleared, silently defeating the buffering feature specifically for the slide→jump/slide chain, no matter when during the slide the player pressed. Fixed by tuning `SLIDE_RECOVERY_MS` down to 100 (still a real, perceptible anti-spam pause, now safely under the buffer window).

**Verification performed** (all against the actual shipped modules, imported directly into a Node smoke test with minimal `window`/`document`/`fetch`/`Request` stubs — not a reimplementation):
1. `npm run build` succeeds cleanly (77 modules).
2. **Jump apex**: measured by stepping the real `Player.update()` loop from `_startJump()` to landing — lands at exactly 2.0000, matches `jumpForce²/(2·|gravity|)` to floating-point precision.
3. **State-driven hitbox**: `writeHitboxBox3()` checked directly for all three states — RUNNING/JUMPING both 2.0 (JUMPING's bottom confirmed to track `mesh.position.y`, i.e. "raised by jump arc" rather than a separately-sized box), SLIDING is 0.9 and its top (0.9) confirmed ≤ DRONE_LOW's 1.1 bottom. Cross-checked against the full obstacle taxonomy from the plan: at measured jump apex, hitbox bottom (2.0) is confirmed ≥ BARRICADE_LOW's 1.0 top (clears) and < DRONE_HIGH's 2.6 (does not clear) — the exact "each obstacle's only valid answer is physically true" guarantee Milestone 5 depends on.
4. **Input buffering**: a keydown fires and is consumable immediately; a second `consumeBuffered` call for the same entry correctly returns false (no double-consumption); a buffered action correctly expires and is unconsumable after `INPUT_BUFFER_MS` elapses (real ~170ms wait); a jump buffered during a slide is correctly *not* consumed while blocked, *not* consumed during the post-slide recovery cooldown either (confirmed this is intentional — buffering bypassing an anti-spam cooldown would make the cooldown pointless), and correctly fires the instant both clear.
5. **CollisionSystem swept detection**: constructed a synthetic obstacle that jumps from z=-1 to z=+5 in a single large-delta (0.15s) frame simulating a stutter — confirmed the swept/sub-stepped check catches it (reports the correct hit payload, hides the item), with an explicit control assertion proving a naive "check only the final z" approach (final z=5, outside the ±2 band) would have missed it entirely.
6. **Live browser verification still not possible from this sandbox** (same limitation as Milestones 1-3) — the physics, hitbox math, buffering, and collision sweep are all verified numerically against the real code; how jumping/sliding *feels* in motion needs a real `npm run dev` look when convenient.

## Milestone 5 — Obstacles, patterns, difficulty — ✅ DONE

**Goal:** Replace random single-lane, single-item spawning with the plan's mixed 4-obstacle taxonomy, an authored `PatternLibrary` of track segments (obstacles + coin trails), and a `SpawnDirector` that selects patterns with variety enforcement, an automatic solvability guarantee, and a within-level difficulty ramp (speed + density) — while preserving Milestone 4's "every obstacle has exactly one valid escape" guarantee.

- [x] `GameConfig.js` gained the full M5 tuning surface: `OBSTACLE_TYPES` (lane span + height band + escape action for each of the 4 types, tuned jointly against M4's `PLAYER_HITBOX`), `DIFFICULTY_RAMP` (speed/density multipliers + ramp distance), `REACTION_BASE_SPEED` (the fairness-scaling reference point), `MIN_OBSTACLE_GAP_SECONDS`, `PATTERN_NO_REPEAT_WINDOW`.
- [x] `src/game/entities/Obstacles.js` — `ObstacleFactory`: shared geometry/materials built once per type in the constructor, `createInstance(type)` builds a lightweight per-instance Group + `update(time)` closure. Final design: 3-4 meshes/instance (a single spinning-torus "rotor ring" standing in for a 4-arm/4-rotor assembly), not the 10-11-mesh version first drafted — see below. Only rotation and emissive intensity are ever animated, never position, since `CollisionSystem` (M4) tests the obstacle's live `Box3.setFromObject`.
- [x] `src/game/world/PatternLibrary.js` — 13 authored patterns (`{id, difficulty, skills, build()}`), each a full ~20-unit segment combining obstacle placement AND a coin trail. Lane roles (`randomLane()`, `randomLaneExcept()`, `randomAdjacentPair()`) resolve to concrete lane indices at `build()` time, so a small authored set yields much more real variety than its raw count. Covers solo (1 obstacle), double (2, same z), combo (2, staggered z), a 3-obstacle `gauntlet-three`, and a 0-obstacle `empty-coin-trail` breather.
- [x] `src/game/world/SpawnDirector.js` — `checkSolvability(obstacles)` (exported standalone, group-by-z + "at least one non-switch-escape lane per z-group") is the concrete mechanism behind the plan's "randomly generating an impossible wall becomes structurally impossible." `SpawnDirector` owns the intra-level ramp (`getRampedSpeed`, `getDensityFactor`, `getMaxDifficulty`, all distance-driven via `advance(moveDist)`/`resetForLevel()`) and `selectPattern(maxDifficulty, currentSpeed)`, which filters by difficulty, enforces a no-immediate-repeat window (relaxed on its last 2 attempts so it can't spin when the eligible pool is smaller than the window), filters out patterns whose authored spacing is too tight for the current speed, re-verifies solvability on the resolved (concrete-lane) result, and falls back to the always-safe `empty-coin-trail` if all attempts fail.
- [x] `src/game/world/WorldStreamer.js` — rewritten to integrate both: each of the 15 pooled chunks now carries 3 obstacle slots (each pre-building all 4 type variants, so any slot can show any type across recycles with zero mid-game allocation) and 3 coin slots, sized to the densest authored pattern (`gauntlet-three`). On recycle, `_refreshChunkContent()` deactivates everything then calls `spawnDirector.selectPattern()` and activates/positions the returned obstacles (resolving `BARRICADE_WIDE`'s 2-lane span to the midpoint x of its lane pair) and coins; pain-point naming for whichever obstacle type got picked still draws randomly-with-replacement from the level's blocker list, matching the old single-blocker model since pain-point identity is independent of visual obstacle shape. `update()` now recomputes `this.speed` every frame via `spawnDirector.getRampedSpeed(levelBaseSpeed)` (levelBaseSpeed stored separately, set by `setLevel()`), calls `spawnDirector.advance(moveDist)`, and scales the ~50% scenery/interactable density chance by `getDensityFactor()` (capped at 0.9). `setLevel()` now also calls `spawnDirector.resetForLevel()`.
- [x] `src/game/world/SceneryInstancer.js` — small, backward-compatible addition: `rerollChunk(manifest, hasSceneryOverride)` now accepts an optional override so WorldStreamer's ramped density decision drives BOTH scenery and interactable density from one shared roll, instead of two independent 50% coin flips that previously happened to usually agree. Falls back to the original independent roll when omitted.
- [x] End-to-end verification — see below.

**Two issues self-caught and fixed during implementation (no user messages occurred this milestone — these were found via my own arithmetic/reasoning checks, not reported bugs):**
1. **`MIN_OBSTACLE_GAP_SECONDS` was initially 0.35, incompatible with authored pattern spacing.** Worked the arithmetic: at level-1 base speed (30 u/s), a 0.35s gap requires 10.5 world units between obstacle z-groups — but `gauntlet-three` only had 6-unit gaps, meaning my own densest pattern would have been rejected by `selectPattern` even at the easiest speed. Fixed two ways: lowered the constant to 0.15 (real, but now acts as a speed-adaptive safety valve that excludes tight combos only once speed makes them genuinely unfair, rather than a blanket ban at any speed), and widened `gauntlet-three`'s z-offsets from ±6 to ±8 for extra margin.
2. **Drone obstacles were originally over-complex (10-11 meshes/instance)** — a first draft used 4 separate arm+rotor mesh pairs. Recognized this risked the ~15-draw-call scenery budget Milestone 3 established, especially since `double-drone-*` and `gauntlet-three` patterns can put multiple drones on screen at once. Replaced with a single spinning `TorusGeometry` "rotor ring" (reads as a motion blur) — 3-4 meshes/instance instead.

**Verification performed** (against the actual shipped modules — two Node scripts, 54 checks total, all passing):
1. `npm run build` succeeds cleanly (80 modules, asset budget gate unaffected at 11.37 MB).
2. **Solvability**: `checkSolvability` (the real exported function) confirmed to reject a deliberately-constructed all-3-lanes-blocked-by-switch-only-escapes wall (and correctly reports the offending z), accept a wall with a jump/slide escape lane, and accept an empty list. All 13 authored patterns in `PatternLibrary` built and solvability-checked across 500 random lane-role resolutions each (6,500 builds total) — zero unsolvable results.
3. **Height-band cross-check against Milestone 4's physics, numerically, not by re-asserting comments**: recomputed jump apex from `PLAYER_PHYSICS` (confirmed exactly 2.0), then checked for each of the 4 `OBSTACLE_TYPES` that jump clears it if and only if its `escape` is `"jump"`, slide clears it if and only if `escape` is `"slide"`, and neither clears the 2 `"switch"`-escape types (`BARRICADE_WIDE`, `DRONE_HIGH`) — 26 individual assertions, all passing. Also confirmed the default grounded `RUNNING` hitbox overlaps all 4 types (nothing is passable by standing still).
4. **Difficulty ramp**: `getRampedSpeed`/`getDensityFactor`/`getMaxDifficulty` all confirmed to return exactly their base values at distance 0, a correct linear-interpolated value at 50% of `rampDistance`, and correctly cap at their maximums (never exceed) after a 10x-rampDistance overshoot; `resetForLevel()` confirmed to zero the ramp back out. Confirmed a pattern exists at every difficulty level 1 through 4 (so `getMaxDifficulty`'s full range is actually reachable/useful). `dynamicLookAheadZ` confirmed to scale exactly linearly with speed (the difficulty-axis fairness guarantee).
5. **Variety + fairness under load**: 300 consecutive `selectPattern` draws produced zero immediate repeats and drew from a real variety of pattern IDs; 100 draws at a deliberately extreme speed (level-3 base × full in-level ramp × 1.6, an unrealistic worst case) all still returned solvable patterns, confirming the gap-filter/fallback chain never leaves a chunk unresolved.
6. **Real `WorldStreamer` integration**, constructed directly (stub scene, no WebGL needed) and run through `setLevel(1)` + 400 large-delta `update()` ticks to force many recycles: confirmed exactly 15 chunks × 3 coin slots × 3 obstacle slots (each pre-building all 4 type variants) with everything starting hidden; confirmed obstacle AND coin slots both actually activate over the run; confirmed every activated obstacle carries `CollisionSystem`-compatible `userData` (`isInteractable`/`type`/string `name`/`text`/`consequence`); confirmed every activated `BARRICADE_WIDE` sits at exactly ±1.5 (the midpoint of an adjacent lane pair) and every single-lane obstacle sits exactly on one of the 3 real lane x-positions; confirmed speed actually ramps upward over the run and never exceeds `levelBaseSpeed × 1.25`.
7. Static grep confirmed no stale references remain to the old single-coin/single-blocker model (`chunkBlockers`, `_refreshInteractable`, `_createBlockerPoolObject`, `isFlying`, etc.) anywhere in `src/`.
8. **Live browser verification still not possible from this sandbox** (same limitation as Milestones 1-4) — solvability, the ramp, and the pooling/positioning integration are all verified numerically against the real code; how the 4 obstacle types read visually and how the ramp *feels* over a real playthrough need a real `npm run dev` look when convenient.

## Milestone 6 — Coins, power-ups, scoring — ✅ DONE

**Goal:** Fix the unwinnable level at its root (not just paper over the symptom), give coin trails a real jump-reward shape, implement the two themed power-ups as genuine gameplay mechanics, and add a working `ScoreSystem`.

- [x] **The unwinnable-level fix, done properly.** `WorldStreamer.setLevel()` no longer builds 3 duplicated copies of a level's features and shuffles them together (the actual root cause: independent-random-sampling-with-heavy-duplication is a coupon-collector problem, requiring ~81 collects for 22 unique features per the plan's own math). It now deals a proper shuffled bag *without replacement* (`_shuffledFeatureBag()`), and `_nextFeature()` automatically reshuffles and refills the bag once it empties. This gives two guarantees, both verified numerically (not just asserted): every consecutive window of `features.length` draws contains every unique feature at least once (true no-replacement, not just "eventually appears"), and a feature drawn once is always offered again in a later cycle — so a missed coin is never a permanent loss and no level can become unwinnable regardless of player skill.
- [x] **A second, self-discovered problem: naive dense dealing completed levels in ~12-15s, not the plan's ~60-75s.** Fixing the bag alone made the level *winnable* but not *paced* — WorldStreamer's per-recycle multi-slot coin population (up to 3 slots, most chunks, every ~0.667s) drained all 22 features almost immediately. Caught this by actually simulating a run against the real code rather than assuming the bag fix was sufficient. Fixed with `FEATURE_SPACING_DISTANCE` (90 units, derived from the plan's own "~2000 units / 22 required" estimate): a new `_distanceSinceLastFeature` gate in `WorldStreamer` means a coin slot only deals a *new* feature once enough distance has passed since the last one; a gated slot just shows no coin that time (a normal-looking sparser trail, not a broken one). Re-simulated after the fix: Level 1 completes in 69.5s (perfect catcher) to 74.4s (35%-catch-rate catcher) — squarely in the target range. Levels 2 and 3 also checked (52.7-100s and ~21s respectively — Level 3's short time matches its "final 10 features, everything at once" design intent).
- [x] **Coin-trail jump-arc shape.** `PatternLibrary.js` coins gained an optional `y` field (default: WorldStreamer's usual 1.2). Every coin that coincides with a `BARRICADE_LOW`'s own `(lane, z)` — `solo-barricade-low`, `combo-low-then-wide`, `combo-wide-then-low`, `gauntlet-three` — is raised to `COIN_JUMP_REWARD_HEIGHT` (2.0, matching the jump apex from Milestone 4), so the trail visually traces the jump arc and actually sits inside the airborne hitbox at the moment that matters, instead of a coin at running height that's grabbed by simply standing in that lane. Confirmed these solo/combo patterns already double as the plan's "risk/reward" coin-trail shape by construction (M5's own design): the reward coins sit in the obstacle's own lane, never the safe adjacent one, so grabbing them means committing to clearing the obstacle rather than switching around it — no new patterns were needed to deliver this, just the height fix. "S-curve" trails are already satisfied structurally by existing multi-coin patterns placing coins in different lanes across z (e.g. `combo-low-then-wide`).
- [x] **Two themed power-ups, both real mechanics, not just flags.** `GameConfig.js`'s `POWER_UPS` maps the two named Level-2 features to their effect (`"Automated Collection"` → magnet, 8s; `"Payment Gateway Fallback Mechanism"` → shield, one hit). `WorldStreamer` detects a power-up feature when dealt and gives its coin a distinct glowing material (`powerUpMat`) plus a pulse in `update()`, and forwards `powerUp`/`powerUpDurationMs` through the coin's userData.
  - **Magnet**: `Player.activateMagnet(durationMs)` sets `hasMagnet` + a countdown timer (re-collecting refreshes rather than stacking). `CollisionSystem._overlapsPlayer()` widens the player's collision box to the full lane span (`MAGNET_HALF_WIDTH`, reusing Milestone 2's `FRAMING.laneSpanX/2`) for coin checks only while active — a real widened-pickup mechanic, verified by placing a coin 2 lanes away and confirming it's missed without magnet, collected with it.
  - **Shield**: `Player.activateShield()` sets `hasShield`. `takeHit()` now checks it first and, if present, consumes it and returns `false` (absorbed: no life lost, no hit-reaction lock/red-flash) instead of `true` (real damage). This required a real architectural fix, not just a Player-side flag: `CollisionSystem._reportHit()` now reports the outcome type as `"shielded"` vs `"blocker"` based on `takeHit()`'s return value, and **`App.vue`'s `handleCollision` needed a new branch** for `"shielded"` — without it, the AUTHORITATIVE `gameStats.lives` counter (separate from `Player`'s own, currently-unused `this.lives`) would still decrement even when the shield absorbed the hit, silently breaking the entire mechanic. Verified end-to-end through the real `CollisionSystem` with a shielded player and a real blocker mesh.
  - Both auras are simple always-present, hidden-when-inactive meshes on the player (translucent blue sphere for shield, glowing gold ring for magnet) — enough to make the mechanic legible during testing now; HUD icons with radial timers are explicitly Milestone 8's job per the plan's own table, not this one.
- [x] `src/game/systems/ScoreSystem.js` (new) — engine-owned, fed purely by coin-hit events via `Engine.js`'s new `_handleHit()` wrapper around `CollisionSystem`'s `onHit` callback. Awards `SCORE_POINTS.coin` (100) per coin plus `SCORE_POINTS.exclusiveBonus` (50) for exclusive features, counting every coin grabbed (including an already-owned feature re-dealt by a later bag cycle) since score rewards reflexes independent of `levelFeaturesCollected`'s "is this new required progress" count. `Engine.js` also gained `resetRun()` (resets `scoreSystem` and, opportunistically, the pre-existing-but-dead `player.lives`), called from `App.vue`'s `restartGame()` so score doesn't silently carry over into a new attempt.
- [x] `App.vue`: `gameStats.score` added and updated from the enriched coin payload; power-up activation gets a one-line addition to the existing "Yay! {feature}!" popup text (🧲/🛡️) rather than a new popup mechanism. Deliberately did **not** touch the leaderboard's time-string storage format or build any new score/power-up UI (icons, radial timers) — those are Milestone 8 concerns per the plan's own table; this milestone only had to make the underlying data and mechanics real and correctly wired.
- [x] End-to-end verification — see below.

**Verification performed** (against the actual shipped modules — three Node scripts, 180 checks total, all passing, plus a full regression pass of every prior milestone's Node suite):
1. `npm run build` succeeds cleanly (81 modules, asset budget gate unaffected at 11.37 MB).
2. **Bag mechanics**: `_nextFeature()` on a real `WorldStreamer` after `setLevel(1)` never returns undefined across 132 consecutive draws; every consecutive 22-draw window contains all 22 unique names (the actual no-replacement guarantee); a name drawn once reappears in a later cycle (refill works); a perfect player collects all 22 uniques in exactly 22 draws (not a ~68-draw probabilistic expectation) — directly disproving the old coupon-collector bug against the new code.
3. **Pacing**: simulated Level 1/2/3 playthroughs using the real `WorldStreamer.update()` loop under both a near-perfect and a 35%-catch-rate assumption. Level 1 lands at 69.5-74.4s, Level 2 at 52.7-100s, Level 3 at ~21s — all levels complete (bag refill guarantees this even for the weak catcher), and Level 1/2 land in the plan's ~60-75s-per-level ballpark.
4. **Power-ups**: confirmed via the real `Player` + `CollisionSystem` together — a coin 2 lanes away is missed without magnet and collected with it active; magnet expires on its own after `durationMs`; a shielded hit returns `false` from `takeHit()`, costs no life, sets no hit-reaction lock, and consumes the shield so the *next* hit is real damage; `CollisionSystem` reports `"shielded"` (not `"blocker"`) for an absorbed hit, with the popup-relevant text/consequence fields still present.
5. **ScoreSystem**: point math (100/150 with exclusive bonus), running total across multiple coins, `reset()` correctness.
6. **Config sanity**: both `POWER_UPS` keys match real feature names in `GameContent.js` (not silently dead due to a typo); `MAGNET_HALF_WIDTH` correctly reuses `FRAMING.laneSpanX/2`.
7. **Coin jump-arc heights**: every `BARRICADE_LOW`-coinciding coin across all patterns and 100 random resolutions each is raised to exactly `COIN_JUMP_REWARD_HEIGHT`.
8. **Full regression pass**: re-ran every Milestone 5 Node suite (solvability, pattern library, difficulty ramp, `WorldStreamer` pooling) unchanged and confirmed all still pass; separately re-verified Milestone 4's jump apex (~2.0) and sliding hitbox height (0.9) still hold against the edited `Player.js`, and that `takeHit()` without a shield still behaves exactly as before (returns `true`, decrements a life).
9. **Live browser verification still not possible from this sandbox** (same limitation as Milestones 1-5) — the bag/pacing/power-up mechanics are verified numerically and via simulation against the real code; how the raised jump-reward coins and power-up auras actually look, and how the pacing *feels* in a real playthrough, need a real `npm run dev` look when convenient.

## Milestone 7 — 4 characters
Not started. Scope: `CharacterLoader.js`, shared Mixamo skeleton + single shared `animations.glb`, 4 characters (1 male suit, 1 female suit, 2 anime) sourced + optimized, `CharacterSelect.vue` with live 3D turntable.

## Milestone 8 — UI build-out and polish
Not started. Scope: HUD upgrades (GSAP counters, success-popup fix), `PauseMenu.vue`, rebuilt `GameOver.vue`, power-up icons, **Landing.vue (Lobby) + RegistrationForm.vue (Registration) animation and responsiveness pass** — both explicitly kept, never deleted.

## Milestone 9 — Juice, audio, onboarding
Not started. Scope: particles/shake/hitstop, `AudioManager.js` + local audio files (replacing remote SoundHelix URL), interactive tutorial, TV attract mode, idle timeout, content-script corrections (drone line contradiction, per-feature `exclusiveLine`), move (not delete) `MainMenu.vue`/`DemoBooking.vue`/`PrizeWheel.vue` to `src/ui/_unused/`.

## Milestone 10 — Device QA, perf validation, docs
Not started. Scope: full responsiveness matrix pass, performance gates (≤20MB, ≥55fps mid-range Android, 60fps iPad, ≤120 draw calls), corrected `initial_implementation_documentation.md`.

---

## Notes / Issues

- **Model switched mid-session**: session started on Opus 5, user switched to Sonnet 5 (effort: max) partway through Milestone 1 kickoff. This tracker file was created at that point specifically so context survives model/session switches.
- **Docs are contradictory on drones** — `HowToPlay.vue` says "swipe down to slide under drones," but the Simple Overview and Level 2 intro card say lane-switch only. User resolved this: mixed system (some drones slide, some lane-switch) — see Milestone 5/9. The content script doc itself still needs the corresponding line fixed (Milestone 9).
- **`initial_implementation_documentation.md` is inaccurate** — claims a React migration; codebase is actually Vue 3 + Pinia. Pinia is installed but currently unused. Correcting this is Milestone 10, not urgent before then.
- **Lead capture is currently just `console.log`** in `App.vue` — real CRM endpoint/credentials needed before Dreamforce. Not blocking early milestones.
- **Brand colors unconfirmed** — marketing overview says exact blue/gold should come from a Figma file. Current hardcoded values (`#042C53`, `#F4C775`, `#00B0FF`) are centralized in `style.css`, so swapping later is low-cost.
- **Character source assets and audio assets both need commercial licensing** — flagged as open items, not blockers for the code/pipeline work.
- **Never delete**: `Landing.vue` (Lobby) and `RegistrationForm.vue` (Registration) are required and must be kept + polished, not removed, even though nothing in the 3D rewrite scope directly forces their existence.
- **Full plan lives at** `docs/IMPLEMENTATION_PLAN.md` — this tracker is deliberately a thin status layer on top of it, not a duplicate. Refer back to the plan for the "why" behind any task.
- **claude-in-chrome cannot reach this sandbox's localhost** — confirmed during Milestone 1 verification: navigating to `127.0.0.1`/`localhost` dev server URLs fails ("showing error page") even with the server confirmed reachable via `curl` from this same sandbox, while a public URL (example.com) loaded fine in the same tab. The browser the extension drives and this sandbox's `npm run dev` are in different network contexts. For any future UI/visual verification, either ask the user to check `npm run dev` themselves, or find another way to bridge — don't assume the browser tools can reach a locally-started dev server here.
- **`assets:optimize` is rerunnable** — `scripts/optimizeAssets.js` always reads fresh from `assets-src/` and regenerates `public/assets/` + `public/textures/grass_normal.jpg` from scratch; originals in `assets-src/` are never touched. Safe to re-run any time tuning needs to change (e.g. if a later milestone wants a different simplification ratio).
- **`Engine.js` now lives at `src/game/core/Engine.js`**, not `src/game/Engine.js` — moved during Milestone 2 to match the target architecture (`core/`, `world/`, `entities/`, `systems/` subdirectories). **`WorldGenerator.js` is gone as of Milestone 3** — fully replaced by `src/game/world/WorldStreamer.js` (+ `TrackBuilder.js` + `SceneryInstancer.js`). **`Player.js` is gone as of Milestone 4** too — moved to `src/game/entities/Player.js` and rewritten around the state machine. The `src/game/` top level now holds only `config/`, `core/`, `world/`, `entities/`, `systems/` — no more loose files at that level, fully matching the plan's target tree.
- **Jump physics use analytic position, not step-by-step integration** — `Player.js`'s `movementState === JUMPING` branch computes `y(t) = jumpForce*t + 0.5*gravity*t²` directly from elapsed jump time, NOT `velocity += gravity*dt; position += velocity*dt` per frame. This was a deliberate fix, not a style choice: Euler stepping measurably undershoots the true apex, worse at lower framerates (verified: -5.3% at 60fps, -15.3% at 20fps), which silently made the game easier on smooth devices and harder on struggling ones. **If any future milestone touches jump/fall physics again (e.g. Milestone 9's landing-squash juice), keep the analytic-formula approach** — reverting to per-frame integration would reintroduce the framerate-dependent-difficulty bug.
- **`SLIDE_RECOVERY_MS` (100) must stay below `INPUT_BUFFER_MS` (120)** — this isn't an arbitrary pair of values; the recovery cooldown can only ever be bypassed by a legitimately-still-buffered action if `INPUT_BUFFER_MS >= SLIDE_RECOVERY_MS`. If either constant is retuned later (e.g. Milestone 9 polish), keep this inequality true or buffering through a slide→jump/slide chain will silently stop working again (found and fixed once already in Milestone 4).
- **Building GLBs ship 3 LOD variants at identical transform** (`"L_Build_1"`/`"_LOD1"`/`"_LOD2"`, etc.) — discovered during Milestone 3 while designing `SceneryInstancer`. The pre-M3 code's generic "traverse all meshes, apply material to each" pattern was rendering all 3 LOD levels stacked simultaneously for every building. If Milestone 7's character work or any future asset touches a model with suspicious multiple same-position child meshes, check whether it's LOD data before assuming they're separate parts that all need rendering — `SceneryInstancer.js`'s `buildBuildingVariant()` has the "pick highest-vertex-count mesh" pattern already, reusable elsewhere if needed.
- **`lookAheadZ` (45 units) is a spawning-rule constant for Milestone 5's `SpawnDirector`, NOT a camera-math input** — this was a real bug I caught and fixed during Milestone 2 (see that section for the full story). If Milestone 5 work ever touches `CameraRig.js` again, do not reintroduce `lookAheadZ` into the FOV distance calculation — the fairness guarantee is satisfied by `SpawnDirector` never placing anything closer than 45 units, independent of camera optics.
- **`Player.lives` is a separate, currently-dead counter from `gameStats.lives` (App.vue)** — pre-existing since Milestone 4, not introduced by Milestone 6. `App.vue`'s `handleCollision`/`gameStats.lives` is the ONLY counter that actually drives the game-over check; `player.lives` is decremented in `takeHit()` but never read anywhere else. Milestone 6 added `Engine.resetRun()` (resets `scoreSystem` + opportunistically `player.lives`) but did NOT consolidate the two counters into one — if a future milestone wants to display `player.lives` anywhere or refactor life-tracking, resolve this duplication first rather than trusting either counter blindly.
- **`FEATURE_SPACING_DISTANCE` (90 units) paces new-feature dealing and was tuned by actually simulating a run against the real `WorldStreamer`, not derived from theory alone** — if `PatternLibrary`'s pattern density, `DIFFICULTY_RAMP`'s density multiplier, or the per-chunk coin slot count (`MAX_COIN_SLOTS` in `WorldStreamer.js`) change in a later milestone, re-run the completion-time simulation (see this milestone's verification notes) rather than assuming pacing still holds — it's an empirically-tuned constant, not a formula that self-corrects.
- **The shield mechanic REQUIRED an `App.vue` change, not just a `Player.js` flag** — `CollisionSystem` now reports a blocker hit's outcome as `"shielded"` or `"blocker"` based on `Player.takeHit()`'s return value specifically so `App.vue`'s authoritative `gameStats.lives` counter knows whether to decrement. If a future milestone adds another "avoid damage" mechanic, follow the same pattern (report the OUTCOME type through the payload) rather than trying to have the engine silently suppress the Vue-side life decrement some other way.
- **Power-up coin visuals (glow material, pulse) and auras (shield sphere, magnet ring) are intentionally minimal placeholders** — legible enough to confirm the mechanic works, not final polish. Milestone 8 owns HUD icons with radial countdown timers per the plan's own table; Milestone 9 owns juice (particle bursts, etc.) — don't be surprised these look basic; that's by design, not an oversight.
- **`lookAheadZ`'s "minimum reaction distance" guarantee is satisfied structurally, not by an active per-spawn check** — Milestone 5's `SpawnDirector` never actively measures distance-to-player before placing a pattern; it relies on the chunk-recycle architecture itself (new content only ever instantiates when a chunk recycles to the back of the 15×20 pool, ~270 units from the player, always far beyond even a heavily speed-ramped `lookAheadZ`). `WorldStreamer`'s constructor asserts pool depth against `spawnDirector.dynamicLookAheadZ()` at startup specifically so this claim stays checkable if `poolSize`/`trackLength` or the ramp multipliers are ever changed later — don't remove that check without re-verifying the margin by hand.
- **`MIN_OBSTACLE_GAP_SECONDS` (0.15) is deliberately modest, not a full reaction-time window** — it's a speed-adaptive safety valve that excludes a tightly-spaced authored combo only once speed climbs high enough to make it genuinely unfair, not a blanket per-obstacle-pair reaction-time requirement. If a future milestone authors a new pattern with obstacles closer than ~2 units apart at level-1 speed, it may get excluded even at the easiest difficulty — widen its z-offsets (as `gauntlet-three` needed) rather than lowering this constant further.
- **Obstacle visual complexity is capped at 3-4 meshes/instance by design** (`ObstacleFactory`'s drones use one spinning rotor-ring mesh, not 4 separate arm+rotor pairs) — multiple obstacles can be visible simultaneously (`double-drone-*`, `gauntlet-three`), so if Milestone 9's juice pass wants richer obstacle visuals, budget mesh count per instance carefully against the ~15-draw-call scenery target Milestone 3 established, the same way this milestone did.
- **Pain-point (blocker) naming is independent of obstacle visual/physical type** — any of the 4 `OBSTACLE_TYPES` can carry any pain point from the level's blocker list, picked randomly with replacement each time a slot activates (pain points are meant to recur often, unlike coins which are collected without replacement). If Milestone 6 or 9 wants a specific pain point tied to a specific obstacle shape, that's a new constraint, not something the current data model expresses.
- **Portrait camera rig is tuned for math correctness, not yet for feel** — `CAMERA_RIGS.portrait` in `GameConfig.js` (height=9.5, distanceBehind=13) was derived specifically to keep the required FOV under the 78° ceiling at real phone aspect ratios. This is a reasonable starting point but hasn't been eyeballed in a real browser (sandbox limitation, see Milestone 1's note) — if the user reports the portrait camera feels too far/high, that's the first place to look, and there's real math margin to bring it closer (verified safe down to ~distance=11 before the ceiling binds at the narrowest realistic phone aspect).
